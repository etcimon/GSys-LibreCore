# corev-mb SKiDL design flow (custom boards only).
# Install into the build-platform managed venv:
#   pip install -r corev-mb/lib/requirements.txt
#
# The pcbparts.dev MCP client (pcbparts_mcp.py) uses only the Python standard
# library, so it needs nothing here. skidl is required only to run `mb design`
# on a custom board.

skidl>=1.2.0
